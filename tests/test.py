from brain.tools.screenshot import take_screenshot

There is {"a" if process_running else "no"} process running as of now.
                    The mouse is {"in the right position" if mouse_in_right_position else "not in the right position"}.